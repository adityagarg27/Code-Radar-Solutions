#include <stdio.h>
int main(){
    int n;
    scanf("%d", &n);
    for(int i = n; i <= n; i++){
        for(char ch = 'A'; ch <= n; i++){
            printf("%c",ch);
        }
    }return 0;
}
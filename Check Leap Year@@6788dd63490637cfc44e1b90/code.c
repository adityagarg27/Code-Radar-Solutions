#include <stdio.h>
int main(){
    int year;
    scanf("%d", &year);
    if((year%4==0 && a%100!=0)||a%400==0){
    printf("Leap year");
    }
    else if{
        printf("Not a leap year");
    }
    return 0;
}